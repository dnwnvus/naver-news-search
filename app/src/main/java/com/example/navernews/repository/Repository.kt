package com.example.navernews.repository

import com.example.navernews.dataModel.NewsList
import com.example.navernews.retrofit.NaverAPI

class Repository {
    /*
    private val api = retrofit.create(NaverAPI::class.java)

    fun getSearchText(text: String): Single<NewsList> = api
        .getSearchNews(text)
        .subscribeOn(Schedulers.io())
        .observeOn(AndroidSchedulers.mainThread())
    */
}